// TODO: extend if needed
export interface LineChartSeries {
  x: { key: string };
  y: { key: string };
  color?: string;
}

// TODO: extend if needed
export interface LineChartAxes {
  x: { label: string };
  y: { label: string };
}
